{
  "name": "cv-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check-format": "biome format .",
    "build:html": "resumed render dist/resume_public.json -o dist/resume_public.html -t jsonresume-theme-signal",
    "build:pdf": "resumed export dist/resume.json -o dist/resume.pdf -t jsonresume-theme-signal --puppeteer-arg=--no-sandbox --puppeteer-arg=--disable-setuid-sandbox && resumed export dist/resume_public.json -o dist/resume_public.pdf -t jsonresume-theme-signal --puppeteer-arg=--no-sandbox --puppeteer-arg=--disable-setuid-sandbox"
  },
  "trustedDependencies": ["@biomejs/biome"],
  "dependencies": {
    "jsonresume-theme-signal": "github:menil/jsonresume-theme-signal#main"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.4.2",
    "@types/handlebars": "^4.1.0",
    "handlebars": "^4.7.9",
    "puppeteer": "^24.43.1",
    "resumed": "^7.0.0",
    "typescript": "^5.9.3"
  }
}
